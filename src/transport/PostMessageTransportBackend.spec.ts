import PostMessageTransportBackend from './PostMessageTransportBackend';

describe( 'PostMessageTransportBackend tests', () => {
    it( 'can be constructed with no parameters', () => {
        const instance = new PostMessageTransportBackend();
        expect( instance ).toBeDefined();
    } );

    // TODO: add other tests based upon functionality
} );
