import Postis from './postis';

describe( 'postis tests', () => {
    it( 'can be constructed', () => {
        // eslint-disable-next-line new-cap
        const postis = Postis( {} );
        expect( postis ).toBeDefined();
    } );

    // TODO: add other tests based upon functionality
} );
